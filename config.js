var config = {

    cookieSecret : "noblog",
    database : {
        client : 'mongodb',
        connection : {
            host : '127.0.0.1',
            database : 'noblog'
        }
    }


};

module.exports = config;